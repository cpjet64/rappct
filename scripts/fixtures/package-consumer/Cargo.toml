[package]
name = "rappct-package-consumer"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
rappct = { path = "../../..", features = ["introspection", "net", "serde", "tracing"] }
