# Documented Promises Matrix

This matrix treats readable documentation as claims and current code behavior as source of truth. Claims that depend on corrupted modules are not accepted as implemented.

| Promise | Source | Classification | Current evidence | Completion criteria |
| --- | --- | --- | --- | --- |
| Windows-focused Rust AppContainer/LPAC toolkit | `README.md`, `docs/ARCHITECTURE.md`, `src/lib.rs` | partial | crate root and docs support this, but critical modules are corrupted. | Restored source compiles and Windows smoke tests pass. |
| Cross-platform API surface with non-Windows `UnsupportedPlatform` behavior | `docs/ARCHITECTURE.md`, `docs/API.md`, `src/lib.rs`, `src/util.rs`, `src/ffi/mod.rs` | partial | non-Windows stubs exist for some internals; corrupted modules may break compilation. | `cargo test --all-targets --all-features` passes on supported CI targets or documented Windows-only target. |
| `AppContainerProfile::ensure/open/delete` works | `docs/API.md`, `docs/ARCHITECTURE.md`, `legacy/docs/root/EXECUTION-PLAN.md` | broken/unverified | `src/profile.rs` is NUL-corrupted. | Restore `src/profile.rs`; pass `tests/windows_profile.rs` and examples using profiles. |
| `derive_sid_from_name` returns AppContainer SID | `docs/API.md`, tests in `tests/windows_core.rs` | broken/unverified | exported from corrupted `src/profile.rs`. | Windows test `derive_sid_from_name_works` passes. |
| `folder_path` and `named_object_path` helpers exist | `docs/API.md`, `docs/modules/profile.md` | broken/unverified | implementation file corrupted. | Rustdoc and Windows tests verify returned paths. |
| Known capability catalog and derivation | `docs/API.md`, `docs/modules/capability.md`, `src/capability.rs` | implemented/needs revalidation | readable tests cover catalog lookup, derivation, suggestions. | Full all-feature test pass after corrupted files restored. |
| `SecurityCapabilitiesBuilder` supports known, named, LPAC defaults, and use cases | `docs/API.md`, `src/capability.rs`, legacy plan notes | implemented/needs revalidation | readable docs and prior source review show this exists. | Unit/API tests pass; docs examples compile. |
| `launch_in_container` and `launch_in_container_with_io` work | `docs/API.md`, `docs/ARCHITECTURE.md`, `tests/windows_launch.rs` | broken/unverified | `src/launch/mod.rs` corrupted. | Restore launch module; pass Windows launch tests including stdio, LPAC, job, token checks. |
| `LaunchOptions` supports env, cwd, stdio, suspension, job limits, startup timeout | `docs/API.md`, `docs/ARCHITECTURE.md` | broken/unverified | launch implementation corrupted; `src/launch/env.rs` only covers env block helper. | Compile API surface and pass launch integration tests. |
| `merge_parent_env` prevents environment-block footgun | legacy `TODO.md`, docs/API, examples guide | broken/unverified | claimed in docs; implementation likely in corrupted `src/launch/mod.rs`. | Unit tests for env merge pass; launch tests confirm default inheritance. |
| `LaunchedIo::wait(timeout)` and pipe support | `docs/API.md`, `tests/windows_launch.rs` | broken/unverified | depends on corrupted launch module. | `launch_with_pipes_and_echo`, `launch_waits_for_exit_code`, null/inherit stdio tests pass. |
| Job object drop guard kills child on drop | `docs/ARCHITECTURE.md`, `tests/windows_job_guard.rs` | partial/weakly verified | test exists but is `#[ignore]` and opt-in via `RAPPCT_ALLOW_JOB_TESTS`; implementation corrupted. | Non-destructive opt-in CI documentation and manual pass evidence. |
| Token introspection returns AppContainer/LPAC/package/capability SIDs | `docs/API.md`, `docs/modules/token-sid.md`, `tests/windows_core.rs` | broken/unverified | `src/token.rs` appears zeroed. | Restore token module; pass token tests and CLI `acrun whoami`. |
| ACL grants support file, directory, custom inheritance, registry HKCU/HKLM | `docs/API.md`, `docs/modules/acl.md`, `src/acl.rs`, `tests/windows_acl.rs` | implemented/needs revalidation | readable implementation/tests were reviewed earlier. | Pass ACL integration tests on Windows with privilege expectations documented. |
| Network helpers are gated by `net` and require explicit debug confirmation for loopback add | `docs/API.md`, `docs/modules/diag-net.md`, `src/net.rs`, `tests/windows_net.rs` | implemented/needs revalidation | safety latch test exists; mutating tests ignored/opt-in. | Pass non-mutating tests; run opt-in elevated tests before release. |
| Diagnostics warn on missing network caps and LPAC without defaults | `docs/API.md`, `src/diag.rs`, `tests/windows_launch.rs` | implemented/needs revalidation | feature-gated tests exist. | Pass `--features introspection` and all-features tests. |
| FFI RAII wrappers adopted and unsafe comments enforced | `docs/ARCHITECTURE.md`, `src/ffi/*`, `src/lib.rs` | partial | wrappers are readable; many `allow(dead_code)` stubs exist; current crate cannot be verified. | `cargo clippy --all-targets --all-features -- -D warnings` passes after restoration. |
| Coverage is >=95% regions and historically 99.19% filtered regions | `docs/coverage-report.md`, `Justfile` | stale/unverified | report is historical from 2026-02-26; current source corruption invalidates it. | Fresh `just coverage` pass with report committed or attached. |
| CI matrix covers stable, MSRV 1.88.0 through 1.93.0, beta, nightly, feature combos | `.github/workflows/ci.yml`, `docs/ARCHITECTURE.md` | partial | workflow exists but duplicate dependency check is non-blocking and hosted CI omits local deep gates. | Define required gates, remove `|| true` where intended, confirm current pipeline green. |
| CodeQL analysis is enabled | `.github/workflows/codeql.yml` | corrupted/unverified | file output contains unrelated/corrupted XML-like content. | Restore workflow and verify GitHub Actions parses it. |
| Local-only crates.io publish flow | `RELEASE-CHECKLIST.md`, `Justfile`, `scripts/release*.ps1` | partial/stale | scripts exist; checklist has incomplete clean-tree tasks and stale published baseline. | Fresh `just release-gate-log` on clean tree, version check against current crates.io, explicit publish approval. |
| No unattended publish | `docs/TOOLING.md`, `scripts/release.ps1` | implemented | script requires branch `main`, clean tree, and typed `PUBLISH`; auth prompt requires `FORCE` if no credential. | Keep as release policy; test dry-run on clean tree. |
| Documentation generated via rustdoc and mdBook | `docs/TOOLING.md`, `docs/PROGRESS.md` | partial | mdBook was scaffolded, but prior note says `mdbook` was unavailable during generation. | `cargo doc --workspace --all-features --no-deps` and `mdbook build docs --dest-dir book` pass. |

